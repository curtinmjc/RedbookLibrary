---
declared-services:
  redbooklib-SQLDB:
    label: sqldb
    plan: sqldb_free
applications:
- name: redbooklibraryPart2a
  memory: 512M
  path: RedbookLibrary.war
  buildpack: liberty-for-java
  services:
  - SQL Database-demo-curtin